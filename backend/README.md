# ZamLift Backend

Express API with PostgreSQL, JWT auth, role system, trip lifecycle, booking/payment flow, ratings, and Socket.io realtime updates.

## Quick Start

- Copy `.env.example` to `.env`
- Apply `src/db/schema.sql` to PostgreSQL
- Run `npm install`
- Run `npm run dev`
